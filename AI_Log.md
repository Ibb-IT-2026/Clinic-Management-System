# AI Usage Log

## Entry 01

### Date
2026-09-18

### Student
الطالب الأول (مدير المستودع)

### Tool
Gemini AI

### Purpose
توليد الهيكل الأولي لوثيقة المتطلبات الهندسية SRS، ومراجعة التوزيع الهندسي للمهام.

### Prompt Summary
طلبنا من الأداة إنشاء وثيقة متطلبات مصغرة Mini-SRS استناداً إلى الملفات الموجودة حالياً في المشروع.

### AI Suggestions
- استخراج 5 متطلبات وظيفية مباشرة من واجهات النظام.
- إضافة حالات حافة (Edge Cases) تتعلق بتجاوز الصلاحيات وعدم الاتصال بقاعدة البيانات.

### Accepted Suggestions
- تم قبول حالات الحافة وتضمينها في الوثيقة لأنها واقعية.
- تم قبول المتطلبات الوظيفية التي تغطي أساسيات النظام (CRUD).

### Rejected Suggestions
- إضافة متطلبات الدفع الإلكتروني عبر بوابات دفع خارجية.

### Reason for Rejection
- الدفع الإلكتروني يعتبر خارج النطاق المحدد للمشروع الجامعي الحالي.

### Human Review
راجع الفريق المتطلبات واعتمدها لتتوافق بشكل مباشر مع الكود المصدري المكتوب مسبقاً.

---

## Entry 02

### Date
2026-09-19

### Student
الطالب الثاني (مطور الواجهات Frontend)

### Tool
ChatGPT (GPT-4)

### Purpose
المساعدة في تصميم شبكة متجاوبة (Responsive Grid) للوحة التحكم (Dashboard) وكتابة سكريبت الوضع الليلي.

### Prompt Summary
"أريد كود CSS باستخدام Flexbox و Grid لعمل لوحة تحكم تحتوي على 3 بطاقات إحصائيات متجاوبة مع الجوال. وكيف أصنع Toggle للوضع الليلي باستخدام Vanilla JS؟"

### AI Suggestions
- استخدام `display: grid` و `grid-template-columns: repeat(auto-fit, minmax(250px, 1fr))` للبطاقات.
- اقتراح إضافة خاصية `data-theme="dark"` على وسم `<html>` لتفعيل الوضع الليلي.
- استخدام `localStorage` لحفظ تفضيلات المستخدم للوضع الليلي.

### Accepted Suggestions
- تم قبول استخدام Grid للبطاقات لأنه يقلل من كتابة الـ Media Queries.
- تم قبول فكرة `localStorage` لحفظ حالة الوضع الليلي لكي لا يضيع عند تحديث الصفحة.

### Rejected Suggestions
- اقتراح استخدام مكتبة خارجية مثل TailwindCSS لتسريع العمل.

### Reason for Rejection
- متطلبات المشروع تنص على استخدام Vanilla CSS لضمان فهمنا العميق للتنسيقات.

### Human Review
قمت بنسخ الكود الخاص بالـ Grid وتعديل الألوان لتتناسب مع هوية العيادة (أزرق وأبيض)، وقمت باختبار الوضع الليلي يدوياً وتأكدت أنه يعمل بدون مشاكل.

---

## Entry 03

### Date
2026-09-19

### Student
الطالب الثالث (مطور الواجهة الخلفية Backend)

### Tool
Claude 3.5 Sonnet

### Purpose
كتابة دوال التحقق من صحة البيانات (Validation) وتحسين أمان استعلامات قاعدة البيانات في `api/patients.php`.

### Prompt Summary
"لدي كود PHP يستقبل بيانات المريض عبر POST ويحفظها في قاعدة بيانات MySQL، كيف أقوم بإضافة Validation صارم لمنع الأسماء الفارغة وكيف أحمي الكود من ثغرات SQL Injection؟"

### AI Suggestions
- استخدام `trim()` و `mb_strlen()` للتحقق من طول الأسماء.
- استخدام `PDO Prepared Statements` مع علامات الاستفهام `?` في استعلامات `INSERT` و `UPDATE`.
- اقتراح استخدام `filter_var` للتحقق من صحة رقم الهاتف.

### Accepted Suggestions
- تم قبول وتنفيذ فكرة الـ `Prepared Statements` بالكامل لأنها تغلق ثغرات الـ SQL Injection.
- تم استخدام التحقق من الطول للأسماء للتأكد من عدم إدخال أسماء وهمية.

### Rejected Suggestions
- اقتراح إضافة طبقة (Middleware) للتحقق من تسجيل الدخول لكل طلب API.

### Reason for Rejection
- المشروع مبني بـ Vanilla PHP ولا يحتوي على نظام توجيه (Router) متقدم يدعم Middleware، وتمت الاستعاضة عن ذلك بالتحقق من الجلسة في بداية الملف.

### Human Review
الكود المقترح للـ Validation كان ممتازا، قمت بتطبيقه في `patients.php` واختبرته بإرسال بيانات خاطئة عبر Postman وتأكدت أن الخادم يرد برسالة خطأ (Error) كما هو مطلوب هندسياً.
